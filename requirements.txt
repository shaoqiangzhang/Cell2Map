datatable
# Pinned requirements for Cell2Map (CPU-focused reproducible setup)
# NOTE: PyTorch and PyTorch-Geometric (PyG) are platform- and CUDA-version-specific.
# Install PyTorch first following https://pytorch.org/get-started/locally/ and then
# install PyG using the matching wheels from https://pytorch-geometric.readthedocs.io/.

numpy==1.24.3
scipy==1.10.1
pandas==1.5.3
scikit-learn==1.1.3
anndata==0.9.1
scanpy==1.9.1
matplotlib==3.7.1
tqdm==4.65.0
datatable==1.1.0
h5py==3.8.0

# Jupyter / notebook reproducibility
jupyterlab==3.6.3
papermill==2.4.1

# Visualization helpers
seaborn==0.12.2
scikit-image==0.20.0

# PyTorch & PyG (commented):
# Install torch first using the official selector. Example CPU wheel:
#   pip install torch==2.2.0+cpu torchvision==0.15.2+cpu torchaudio==2.2.0 --index-url https://download.pytorch.org/whl/cpu
# After installing torch, install PyG following the instructions and wheels at:
#   https://pytorch-geometric.readthedocs.io/en/latest/notes/installation.html
# Typical PyG components (install matching wheels):
#   torch-scatter, torch-sparse, torch-cluster, torch-spline-conv, torch-geometric

# If you need a GPU-enabled reproducible file, tell me your CUDA version and I will
# add pinned torch + PyG wheels/commands appropriate for that platform.
